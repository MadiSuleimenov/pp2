# config.py — Database connection settings
# Change these to match your PostgreSQL / pgAdmin setup.

DB_CONFIG = {
    "host":     "localhost",
    "port":     5432,
    "dbname":   "phonebook",   # create this DB in pgAdmin first
    "user":     "postgres",
    "password": "1234",
}